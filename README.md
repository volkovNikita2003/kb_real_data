# Автоматическая обработка реальных данных

`real_data_auto` — проект обработки экспериментальных данных дифракционного
анализатора размера частиц.

Цель проекта — запускать калибровку, прямое численное моделирование и
восстановление распределения частиц без редактирования исходного кода. Каждый
этап получает путь к директории эксперимента, читает данные и параметры из
заранее определённых мест и сохраняет воспроизводимый результат в `output/`.

## Термины

### Эксперимент

Эксперимент объединяет измерения, для которых одинаковы:

- параметры оптической схемы;
- положения и характеристики детекторов;
- длина волны;
- оптические свойства частиц и среды;
- калибровка;
- аппаратная матрица.

Ответственность за выполнение этих условий несёт пользователь.

### Измерение

Измерение — один набор зарегистрированных сигналов конкретного образца внутри
эксперимента. Один эксперимент может содержать несколько измерений, например
`kmk_15`, `kmk_100` и `kmk_270`.

### Профиль восстановления

Профиль восстановления — именованный набор параметров решения обратной задачи.
Для одного измерения можно выполнить восстановление с несколькими профилями,
например `default`, `camera_only` или `strong_regularization`.

В общем случае восстановление выполняется для пар:

```text
профиль восстановления × измерение
```

## Структура проекта

```text
real_data_auto/
├── README.md
├── src/
│   ├── preprocessing.py
│   ├── build_matrix.py
│   ├── quality_control.py
│   ├── calculate_expected_signals.py
│   ├── calc_darl.py
│   ├── restore.py
│   ├── run.py
│   ├── validate.py
│   ├── validation.py
│   ├── experiment.py
│   ├── parameters.py
│   ├── output.py
│   └── errors.py
└── experiments/
    └── <имя эксперимента>/
        ├── data/
        │   ├── calibration/
        │   │   ├── cam/
        │   │   └── lin/
        │   ├── <имя измерения 1>/
        │   └── <имя измерения 2>/
        ├── input_parameters/
        │   ├── general.yaml
        │   ├── calibration.yaml
        │   ├── darl.yaml
        │   ├── measurements/
        │   │   └── <имя измерения>.yaml
        │   └── restore_profiles/
        │       ├── default.yaml
        │       └── <другой профиль>.yaml
        └── output/                  # создаётся автоматически
            ├── calibration/
            ├── darl/
            │   ├── matrix/
            │   ├── quality_control/
            │   └── expected_signals/
            ├── restore/
            └── archive/
                ├── calibration/
                ├── darl/
                └── restore/
```

## Структура эксперимента

В корне эксперимента допускаются только:

```text
data/
input_parameters/
output/              # необязательная директория результатов
```

Для открытия эксперимента обязательны только `data/` и `input_parameters/`.
Директория `output/` создаётся скриптом обработки при сохранении первого
результата. Если `output` уже существует, он должен быть директорией.

Названия экспериментов, измерений и профилей должны иметь безопасный формат:

```text
^[A-Za-z0-9][A-Za-z0-9_-]*$
```

Это исключает пробелы, служебные символы и неоднозначность при построении путей.

### `data/`

Каталог содержит:

- специальную директорию `calibration`;
- одну или несколько директорий измерений.

Других файлов и директорий непосредственно в `data/` быть не должно.

### `input_parameters/`

Содержит только входные параметры, заданные пользователем:

- `general.yaml` — общие параметры эксперимента и оптической схемы;
- `calibration.yaml` — параметры калибровки;
- `darl.yaml` — параметры прямого численного моделирования;
- `measurements/<имя>.yaml` — необязательное ожидаемое распределение
  конкретного измерения;
- `restore_profiles/*.yaml` — профили восстановления.

### `output/`

Содержит только результаты автоматической обработки:

- `calibration/` — калибровка;
- `darl/` — аппаратная матрица и результаты моделирования;
- `restore/` — результаты восстановления;
- `archive/` — предыдущие версии результатов после запуска с `--force`.

Исходные данные и входные параметры скрипты не изменяют.

## Калибровочные данные

```text
data/calibration/
├── cam/
│   ├── 100.bmp
│   ├── 500.bmp
│   └── 2500.bmp
└── lin/
    └── 15.txt
```

Наличие `cam/` и `lin/` определяется включёнными детекторами из
`general.yaml`.

В `cam/` находятся изображения сигнала калибровочного пинхола. В `lin/`
находится ровно один файл сигнала линейки. Параметры калибровки хранятся
в `calibration.yaml`.

Если данные выключенного детектора присутствуют, валидатор выдаёт
предупреждение. Если данные включённого детектора отсутствуют — ошибку.

## Данные измерения

Полная структура измерения с камерой, линейкой и фоном:

```text
data/<имя измерения>/
├── cam/
├── cam_back/
├── lin/
└── lin_back/
```

Назначение директорий:

- `cam/` — сигнал камеры;
- `cam_back/` — фон камеры;
- `lin/` — сигнал линейки;
- `lin_back/` — фон линейки.

Наличие `cam/` и `lin/` определяется включёнными детекторами. Необходимость
`cam_back/` и `lin_back/` определяется выбранным профилем восстановления.

Правила проверки:

| Данные требуются | Директория существует | Результат |
|---|---:|---|
| да | да | норма |
| да | нет | ошибка |
| нет | да | предупреждение |
| нет | нет | норма |

Так как разные профили могут использовать разные детекторы и фон, полная
проверка выполняется отдельно для каждой пары «профиль × измерение».

## Имена файлов и экспозиции

Время экспозиции задаётся положительным целым числом в микросекундах.

Для камеры:

```text
<экспозиция>.bmp
```

Для линейки:

```text
<экспозиция>.txt
```

Допустимые примеры:

```text
24.bmp
400000.bmp
10.txt
```

Недопустимые примеры:

```text
0.bmp
-24.bmp
24.5.bmp
24us.bmp
024.bmp
```

Экспозиция должна:

- состоять только из ASCII-цифр;
- быть строго больше нуля;
- не содержать ведущих нулей.

Позднее возможна добавка дробных экспозиций.

### Камера и фон

Если профиль использует фон камеры, каждой сигнальной экспозиции должна
соответствовать такая же фоновая экспозиция.

- отсутствие фона для сигнальной экспозиции — ошибка;
- лишняя фоновая экспозиция — предупреждение.

### Линейка и фон

В каждой используемой директории `lin/` и `lin_back/` должен находиться ровно
один TXT-файл. При использовании фона экспозиции сигнала и фона должны
совпадать.

Позднее при необходимости можно будет добавить HDR сборку сигнала с линейки.

## Проверка эксперимента

Валидатор читает параметры и проверяет структуру и имена файлов, но ничего не
создаёт и не изменяет:

```bash
# Все профили и измерения
python src/validate.py experiments/<эксперимент>

# Все профили одного измерения
python src/validate.py experiments/<эксперимент> --measurement kmk_15

# Один профиль для всех измерений
python src/validate.py experiments/<эксперимент> --profile default

# Одна пара «профиль × измерение»
python src/validate.py experiments/<эксперимент> \
  --profile default \
  --measurement kmk_15
```

Флаги `--measurement` и `--profile` можно повторять. Ошибка означает, что
выбранную обработку запускать нельзя. Предупреждение сообщает о лишних или
неиспользуемых данных, но не запрещает запуск.

Для строгой проверки, например в CI, предупреждения можно считать ошибками:

```bash
python src/validate.py experiments/<эксперимент> --warnings-as-errors
```

Чтобы не печатать отдельные предупреждения, используется:

```bash
python src/validate.py experiments/<эксперимент> --no-warnings
```

Итоговая строка всё равно показывает количество скрытых предупреждений. Флаги
`--no-warnings` и `--warnings-as-errors` взаимоисключающие.

Коды завершения:

```text
0 — ошибок нет;
1 — найдены ошибки данных или параметров;
2 — эксперимент нельзя открыть или неверно выбраны профиль/измерение.
```

## Результаты этапов

### Калибровка

Команда:

```bash
python src/preprocessing.py experiments/<эксперимент>
```

Входы:

```text
data/calibration/
input_parameters/general.yaml
input_parameters/calibration.yaml
```

Результаты:

```text
output/calibration/
├── used-parameters.yaml
├── result.yaml
├── quality.yaml
├── camera-calibration-signal.txt
├── line-calibration-signal.txt
├── figures/
└── ...
```

`result.yaml` содержит параметры камеры и линейки, вычисленные при
калибровке и необходимые следующим этапам.

`quality.yaml` содержит диагностические метрики аппроксимации, число
экспозиций и долю корректно собранных HDR-пикселей. Текстовые сигналы содержат
координату, нормированный сигнал и его аналитическую аппроксимацию и подходят
для регрессионного сравнения.

Повторный запуск не изменяет существующий результат. Флаг `--force` переносит
его в `output/archive/calibration/<временная метка>/` и только затем публикует
новый результат. Запись выполняется транзакционно: ошибка расчёта не оставляет
частично заполненную директорию.

Полный формат результата и назначение диагностик описаны в
[docs/calibration.md](docs/calibration.md).

### Построение аппаратной матрицы

Команда:

```bash
python src/build_matrix.py experiments/<эксперимент>
```

Входы:

```text
input_parameters/general.yaml
input_parameters/darl.yaml
output/calibration/result.yaml
```

Результаты:

```text
output/darl/matrix/
├── used-parameters.yaml
├── result.yaml
├── matrix.npz
├── camera_bins.txt
├── line_bins.txt
├── particle_classes.txt
└── ...
```

Калибровка и аппаратная матрица общие для всех измерений эксперимента.
Построение матрицы не зависит от ожидаемых распределений измерений.

### Численный контроль

Команда:

```bash
python src/quality_control.py experiments/<эксперимент>
```

Этап использует готовую аппаратную матрицу, создаёт внутренние контрольные
распределения, моделирует их сигналы, выполняет восстановление и сравнивает
исходные и восстановленные распределения.

```text
output/darl/quality_control/
├── used-parameters.yaml
├── result.yaml
├── distributions/
├── signals/
└── figures/
```

### Ожидаемые сигналы измерений

Если существует файл
`input_parameters/measurements/<измерение>.yaml`, для измерения можно
рассчитать сигнал его ожидаемого распределения:

```bash
# Все измерения с заданными ожидаемыми распределениями
python src/calculate_expected_signals.py experiments/<эксперимент>

# Одно измерение
python src/calculate_expected_signals.py experiments/<эксперимент> \
  --measurement kmk_15
```

Результат сохраняется независимо для каждого измерения:

```text
output/darl/expected_signals/<измерение>/
├── used-parameters.yaml
├── result.yaml
├── expected-distribution.txt
├── expected-signal.txt
└── figures/
```

Отсутствие файла при пакетном запуске означает пропуск измерения. Если
измерение явно выбрано через `--measurement`, отсутствие файла считается
ошибкой.

### Полный расчёт DARL

`calc_darl.py` является агрегирующей командой и последовательно запускает
построение матрицы, численный контроль и расчёт доступных ожидаемых сигналов:

```bash
python src/calc_darl.py experiments/<эксперимент>
```

### Восстановление

По умолчанию выполняются все профили для всех измерений:

```bash
python src/restore.py experiments/<эксперимент>
```

Все профили для одного измерения:

```bash
python src/restore.py experiments/<эксперимент> \
  --measurement kmk_15
```

Один профиль для всех измерений:

```bash
python src/restore.py experiments/<эксперимент> \
  --profile default
```

Одна конкретная пара:

```bash
python src/restore.py experiments/<эксперимент> \
  --profile default \
  --measurement kmk_15
```

Структура результатов:

```text
output/restore/
└── <профиль>/
    └── <измерение>/
        ├── used-parameters.yaml
        ├── result.yaml
        ├── restored-distribution.txt
        ├── reconstructed-signal.txt
        ├── figures/
        └── ...
```

В `used-parameters.yaml` сохраняется полный фактический набор параметров
конкретной пары «профиль × измерение». Он включает:

- общие параметры эксперимента;
- профиль восстановления;
- результаты калибровки;
- сведения об аппаратной матрице;
- детекторы, пути и экспозиции конкретного измерения;
- значения по умолчанию.

### Полная обработка

Команда:

```bash
python src/run.py experiments/<эксперимент>
```

Последовательность:

```text
валидация
→ калибровка
→ построение аппаратной матрицы
→ численный контроль
→ расчёт ожидаемых сигналов
→ восстановление всех пар
```

Следующий этап не запускается, если предыдущий завершился ошибкой.

## Повторный запуск

Существующие результаты не перезаписываются автоматически. Если целевой каталог
уже существует, скрипт завершает работу с сообщением об ошибке.

Для повторного расчёта используется:

```bash
python src/<скрипт>.py experiments/<эксперимент> --force
```

Перед новым расчётом старая версия переносится в `output/archive/`. Архив
повторяет относительную структуру рабочего результата внутри `output/`, а
временная метка добавляется только последним каталогом:

```text
output/archive/
├── calibration/
│   └── 2026-07-24T12-30-45.120450/
├── darl/
│   ├── matrix/
│   │   └── 2026-07-24T13-10-20.318201/
│   ├── quality_control/
│   │   └── 2026-07-24T13-30-10.541120/
│   └── expected_signals/
│       └── kmk_15/
│           └── 2026-07-24T13-45-00.682410/
└── restore/
    └── default/
        └── kmk_15/
            └── 2026-07-24T14-05-17.103482/
```

Например, рабочему результату `output/restore/default/kmk_15/` всегда
соответствует архивный путь
`output/archive/restore/default/kmk_15/<временная метка>/`. Названия этапов,
профилей и измерений при архивировании не изменяются.

Подэтапы архивируются независимо. Для ожидаемого сигнала архивируется только
выбранное измерение, а для восстановления — только заменяемая пара
«профиль × измерение». Соответствующий `used-parameters.yaml` переносится
вместе с остальными результатами. Метка содержит микросекунды, поэтому
последовательные запуски не конфликтуют.

## Назначение модулей `src/`

- `experiment.py` — единое представление структуры эксперимента и построение
  путей;
- `parameters.py` — чтение, проверка и объединение параметров;
- `validation.py` — правила проверки и программный API;
- `validate.py` — командный интерфейс валидатора;
- `output.py` — создание результатов, защита от перезаписи и архивирование;
- `errors.py` — общие типы ошибок;
- `preprocessing.py` — калибровка;
- `build_matrix.py` — построение аппаратной матрицы;
- `quality_control.py` — численный контроль моделирования и восстановления;
- `calculate_expected_signals.py` — сигналы ожидаемых распределений измерений;
- `calc_darl.py` — последовательный запуск трёх подэтапов DARL;
- `restore.py` — восстановление распределений;
- `run.py` — последовательный запуск всего процесса.

## Модель эксперимента

Модуль `experiment.py` реализует классы `Experiment`, `Measurement` и
`RestoreProfile`. Он не читает сигналы и не выполняет вычисления.

Открытие эксперимента:

```python
from experiment import Experiment

experiment = Experiment.open("experiments/17_07_26_kmk_15")
```

При открытии проверяются:

- существование директории эксперимента;
- безопасное имя эксперимента;
- наличие `data/` и `input_parameters/`;
- корректность необязательной директории `output/`;
- отсутствие посторонних элементов в корне;
- наличие `data/calibration/`;
- отсутствие файлов непосредственно в `data/`;
- безопасные имена измерений.

Получение измерений:

```python
experiment.measurement_names()
experiment.measurements()

measurement = experiment.measurement("kmk_15")
measurement.camera_dir
measurement.camera_background_dir
measurement.line_dir
measurement.line_background_dir
```

Получение профилей восстановления:

```python
experiment.restore_profile_names()
experiment.restore_profiles()
experiment.restore_profile("default")
```

Если `input_parameters/restore_profiles/` ещё не создан, эксперимент содержит
пустой набор профилей. Если директория существует, в ней разрешены только
YAML-файлы с безопасными именами.

Стандартные пути результатов строятся без создания директорий:

```python
experiment.calibration_output_dir
experiment.darl_output_dir
experiment.matrix_output_dir
experiment.quality_control_output_dir
experiment.expected_signal_output_dir("kmk_15")
experiment.archive_dir
experiment.archive_path_for(experiment.matrix_output_dir)
experiment.restore_output_dir("default", "kmk_15")
```

Для построения пути результата восстановления указанные профиль и измерение
должны существовать. Это позволяет обнаруживать опечатки до запуска обработки.

Модульные тесты запускаются из корня проекта:

```bash
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
```

Варианты внутренней реализации модели и условия совместимого рефакторинга
описаны в [docs/experiment-model.md](docs/experiment-model.md).

Форматы YAML, обязательные поля и значения по умолчанию описаны в
[docs/parameters.md](docs/parameters.md). Готовые варианты для эксперимента
только с камерой и с камерой и линейкой находятся в `examples/`. Там же есть
набор `minimal/` только с обязательными полями и набор `complete/` со всеми
поддерживаемыми полями.

## Подготовленные эксперименты

В проект добавлены три набора данных для разработки и проверки:

| Эксперимент | Измерения | Детекторы |
|---|---|---|
| `17_07_26_kmk_15` | `kmk_15` | камера и линейка |
| `16_07_26` | `kmk_100`, `kmk_160`, `kmk_270` | только камера |
| `07_07_26` | `kmk_15`, `kmk_100`, `kmk_270` | камера и линейка |

Они позволят проверить:

- эксперимент с одним и несколькими измерениями;
- опциональное использование линейки;
- пакетное восстановление;
- несколько сочетаний профилей и измерений.

## Состояние реализации

В локальной директории experiments/ подготовлены три эксперимента для разработки и проверки. Каталог исключён из Git.

Директории `input_parameters/` предназначены для YAML-файлов параметров.
Директории `output/` предназначены только для файлов, создаваемых скриптами
обработки, и также исключены из Git.

Реализованы:

- модель структуры эксперимента в `experiment.py`;
- общая ошибка структуры эксперимента в `errors.py`;
- модульные тесты модели эксперимента.
- строгие модели и загрузка YAML в `parameters.py`;
- применение значений по умолчанию и проверка согласованности детекторов;
- создание полных фактически использованных параметров этапов;
- схемы и примеры входных YAML-файлов;
- модульные тесты параметров;
- read-only валидатор структуры, детекторов, экспозиций, фона и профилей;
- выбор проверяемых измерений и профилей и режим `--warnings-as-errors`.
- автоматическая калибровка камеры и линейки для старого и нового положения;
- версионированный строгий формат `output/calibration/result.yaml`;
- численные сигналы и показатели качества калибровки;
- транзакционная запись результатов и архивирование с `--force`;
- регрессионная проверка калибровки по эталонному эксперименту.

Следующие модули реализуются в таком порядке:

1. построение аппаратной матрицы в `build_matrix.py`;
2. численный контроль в `quality_control.py`;
3. ожидаемые сигналы в `calculate_expected_signals.py`;
4. агрегирующий запуск DARL в `calc_darl.py`;
5. восстановление в `restore.py`;
6. полный запуск обработки в `run.py`.

Общая инфраструктура `output.py`, первоначально запланированная после
восстановления, реализована вместе с первым вычислительным этапом и далее
переиспользуется всеми командами.
